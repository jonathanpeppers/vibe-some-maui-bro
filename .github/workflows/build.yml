name: Build .NET MAUI App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        # NOTE: macos-latest was 14
        os: [windows-latest, macos-15]
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Install MAUI workloads
      run: dotnet workload install maui --version 9.0.100

    - name: Build
      run: dotnet build VibeSomeMauiBro/VibeSomeMauiBro.csproj --configuration Release
      env:
        CAT_API_KEY: ${{ secrets.CAT_API_KEY }}
